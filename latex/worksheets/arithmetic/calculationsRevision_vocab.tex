% !TeX program = lualatex
% ================================================================
% Tier 3 Vocabulary Key for CalculationsRevision
%
% Generated by the server using the following settings:
%
%   built from          latex/worksheets/arithmetic/calculationsRevision.tex
%   tier 3 vocabulary   green   RGB 0 128 0
%   English text        black   RGB 0 0 0
%   translated text     purple  RGB 112 48 160
%   layout macros       version 2
%   key layout          version 3
%
% Please don't edit any information in this comment block - the server
% compares it against the current settings and rebuilds this file when
% they differ, so changes here would be overwritten. However, feel free
% to make updates to the LaTeX below if you want to edit it.
% ================================================================
% ==== VOCABULARY DATA - generated, do not edit ====
% {"terms":[{"en":"absolute value","def":"A number\u0027s distance from zero, written without a plus or minus sign.","tr":null,"trdef":null},
% {"en":"altitude","def":"Height above or below a reference level, especially sea level.","tr":null,"trdef":null},
% {"en":"BODMAS","def":"A rule telling you to do brackets and powers first, then multiplying and dividing, then adding and subtracting.","tr":null,"trdef":null},
% {"en":"bus stop method","def":"A written layout for dividing, with the dividing number outside the number being split.","tr":null,"trdef":null},
% {"en":"decimal","def":"a number written with a point, where the digits after the point are parts of a whole","tr":null,"trdef":null},
% {"en":"decimal place","def":"The position of a digit after the decimal point.","tr":null,"trdef":null},
% {"en":"decimal point","def":"The dot that separates the whole part from the part less than one.","tr":null,"trdef":null},
% {"en":"divisor","def":"The number that another number is being divided by.","tr":null,"trdef":null},
% {"en":"even","def":"A whole number that can be split exactly into two equal whole numbers.","tr":null,"trdef":null},
% {"en":"factor","def":"a whole number that divides exactly into another number","tr":null,"trdef":null},
% {"en":"lattice method","def":"A written layout for multiplying, using a grid with diagonal lines.","tr":null,"trdef":null},
% {"en":"magnitude","def":"The size of a number ignoring its sign; the same as absolute value.","tr":null,"trdef":null},
% {"en":"negative","def":"Less than zero.","tr":null,"trdef":null},
% {"en":"odd","def":"A whole number that cannot be split exactly into two equal whole numbers.","tr":null,"trdef":null},
% {"en":"operation","def":"A mathematical action on numbers, such as adding, subtracting, multiplying or dividing.","tr":null,"trdef":null},
% {"en":"positive","def":"Greater than zero.","tr":null,"trdef":null},
% {"en":"power","def":"how many times a number is multiplied by itself","tr":null,"trdef":null},
% {"en":"prime number","def":"A whole number greater than 1 whose only factors are 1 and itself.","tr":null,"trdef":null},
% {"en":"recurring decimal","def":"A number with digits after a decimal point that repeat forever.","tr":null,"trdef":null},
% {"en":"remainder","def":"The amount left over after dividing a number into equal groups.","tr":null,"trdef":null},
% {"en":"sign","def":"The plus or minus symbol that shows whether a number is above or below zero.","tr":null,"trdef":null},
% {"en":"whole number","def":"A number with no part after the decimal point.","tr":null,"trdef":null}]}
% ==== END VOCABULARY DATA ====

\documentclass[11pt]{article}
\usepackage[a4paper,margin=18mm]{geometry}
\newcommand{\ealtext}[1]{#1}
\newcommand{\ealtextblock}[1]{{\raggedright #1\par}}
% ================================================================
% EAL layout helpers
% ================================================================
\usepackage{xcolor}

\definecolor{ealkeycolour}{RGB}{0,128,0}
\definecolor{ealenglish}{RGB}{0,0,0}
\definecolor{ealtwocolour}{RGB}{112,48,160}

\newsavebox{\ealboxen}
\newsavebox{\ealboxtr}
\newlength{\ealglosswd}
\newlength{\ealglossraise}
\setlength{\ealglossraise}{1.15em}

% a tier 3 word, in English and in translation alike
\newcommand{\ealkey}[1]{\textcolor{ealkeycolour}{#1}}
\newcommand{\ealkeytr}[1]{\textcolor{ealkeycolour}{\ealtext{#1}}}

% One translated word above one English word. Built from kernel
% primitives, never a tabular, which would break wherever the sheet
% loads array, booktabs or tabularx. The box is as wide as the wider of
% the two, so a long translation pushes its neighbours aside instead of
% overlapping them, and it claims its full height so a table row or a
% TikZ node makes room for it.
\newcommand{\ealgloss}[2]{%
  \sbox{\ealboxen}{\ealkey{#1}}%
  \sbox{\ealboxtr}{\small\textcolor{ealtwocolour}{\ealtext{#2}}}%
  \setlength{\ealglosswd}{\wd\ealboxen}%
  \ifdim\wd\ealboxtr>\ealglosswd\setlength{\ealglosswd}{\wd\ealboxtr}\fi
  \leavevmode
  \makebox[\ealglosswd][c]{%
    \makebox[0pt][c]{\usebox{\ealboxen}}%
    \makebox[0pt][c]{\raisebox{\ealglossraise}{\usebox{\ealboxtr}}}%
  }%
}

% opens up line spacing around a block of glosses, so the raised
% translations sit clear of the line above
\newenvironment{ealglossed}{\par\linespread{2.1}\selectfont\ignorespaces}{\par}

% a whole translated sentence above its English counterpart. block level,
% so it does not belong inside a tabular cell
\newcommand{\ealpara}[2]{%
  \par\addvspace{0.5\baselineskip}%
  {\color{ealtwocolour}\ealtextblock{#1}}%
  \penalty200
  {\color{ealenglish}#2\par}%
  \addvspace{0.5\baselineskip}%
}

% ================================================================
% Vocabulary key layout
% ================================================================
\usepackage{tikz}
\usetikzlibrary{matrix}

\colorlet{ealrulecolour}{ealkeycolour!35!white}

\newlength{\ealwordcol}
\setlength{\ealwordcol}{40mm}
\newlength{\ealmeaningcol}
\setlength{\ealmeaningcol}{\dimexpr\textwidth-\ealwordcol-3mm\relax}

\newcommand{\ealentry}[2]{%
  \par\addvspace{1.6mm}%
  \noindent
  \makebox[\ealwordcol][l]{\textbf{\ealkey{#1}}\hspace{2mm}%
    \textcolor{ealrulecolour}{\dotfill}}%
  \hspace{3mm}%
  \parbox[t]{\ealmeaningcol}{\raggedright\strut #2}%
  \par
}

% The translated word and then its meaning, all inside one \ealtext
% rather than two. A right to left language then orders the word, the
% dash and the meaning itself; set as two runs, the word lands at the
% far end of the line and is read last.
%
% The tie keeps the dash with the word, so a meaning that wraps breaks
% after the dash rather than opening its second line with one.
\newcommand{\ealtrmeaning}[2]{%
  \ealtext{{\bfseries\color{ealkeycolour}#1}~\textemdash{} #2}}

% how a heading and the match-up grid sit on the page
\newcommand{\ealstart}{}

% As many copies of a body as fit go on one page, with a cut line
% between them, so a key of five terms is printed four to a sheet
% rather than taking one to itself. A body too tall to fit is typeset
% directly instead, so it breaks across pages rather than overflowing.
\newsavebox{\ealcopybox}
\newlength{\ealcopyht}
\newlength{\ealcutgap}
\setlength{\ealcutgap}{10mm}
\newcount\ealfits
\newcount\ealdone

\newcommand{\ealcut}{%
  \par\vspace{0.5\ealcutgap}%
  \noindent\hbox to \textwidth{%
    \color{ealrulecolour}%
    \leaders\hbox to 6pt{\hss\rule[0.55ex]{3pt}{0.4pt}\hss}\hfill}%
  \par\vspace{0.5\ealcutgap}%
}

\newcommand{\ealpage}[1]{%
  \sbox{\ealcopybox}{\parbox{\textwidth}{#1}}%
  \setlength{\ealcopyht}{\dimexpr\ht\ealcopybox+\dp\ealcopybox\relax}%
  \ifdim\ealcopyht>\textheight
    #1%
  \else
    \ealfits=\dimexpr\textheight+\ealcutgap\relax
    \ealdone=\dimexpr\ealcopyht+\ealcutgap\relax
    \divide\ealfits by \ealdone
    \ifnum\ealfits<1 \ealfits=1 \fi
    \ealdone=0
    \loop\ifnum\ealdone<\ealfits
      \ifnum\ealdone>0 \ealcut\fi
      \noindent\usebox{\ealcopybox}\par
      \advance\ealdone by 1
    \repeat
  \fi
  \newpage
}

\tikzset{
  ealmatchgrid/.style={
    matrix of nodes, ampersand replacement=\ealnextcol,
    row sep=4mm, column sep=24mm,
    nodes={anchor=west, inner sep=1pt},
    column 1/.style={nodes={text width=38mm, align=left}},
    column 2/.style={nodes={text width=82mm, align=left}},
  },
  ealanswerline/.style={ealkeycolour!70!white, line width=0.7pt},
}

\pagestyle{empty}
\setlength{\parindent}{0pt}

\newcommand{\ealkeybody}{%
  \ealstart{\large\bfseries CalculationsRevision: key vocabulary}\par\medskip
  \ealentry{absolute value}{A number's distance from zero, written without a plus or minus sign.}
  \ealentry{altitude}{Height above or below a reference level, especially sea level.}
  \ealentry{BODMAS}{A rule telling you to do brackets and powers first, then multiplying and dividing, then adding and subtracting.}
  \ealentry{bus stop method}{A written layout for dividing, with the dividing number outside the number being split.}
  \ealentry{decimal}{a number written with a point, where the digits after the point are parts of a whole}
  \ealentry{decimal place}{The position of a digit after the decimal point.}
  \ealentry{decimal point}{The dot that separates the whole part from the part less than one.}
  \ealentry{divisor}{The number that another number is being divided by.}
  \ealentry{even}{A whole number that can be split exactly into two equal whole numbers.}
  \ealentry{factor}{a whole number that divides exactly into another number}
  \ealentry{lattice method}{A written layout for multiplying, using a grid with diagonal lines.}
  \ealentry{magnitude}{The size of a number ignoring its sign; the same as absolute value.}
  \ealentry{negative}{Less than zero.}
  \ealentry{odd}{A whole number that cannot be split exactly into two equal whole numbers.}
  \ealentry{operation}{A mathematical action on numbers, such as adding, subtracting, multiplying or dividing.}
  \ealentry{positive}{Greater than zero.}
  \ealentry{power}{how many times a number is multiplied by itself}
  \ealentry{prime number}{A whole number greater than 1 whose only factors are 1 and itself.}
  \ealentry{recurring decimal}{A number with digits after a decimal point that repeat forever.}
  \ealentry{remainder}{The amount left over after dividing a number into equal groups.}
  \ealentry{sign}{The plus or minus symbol that shows whether a number is above or below zero.}
  \ealentry{whole number}{A number with no part after the decimal point.}
}


\newcommand{\ealmatchrows}{%
  {\textbf{\ealkey{absolute value}}} \ealnextcol {A rule telling you to do brackets and powers first, then multiplying and dividing, then adding and subtracting.} \\
  {\textbf{\ealkey{altitude}}} \ealnextcol {a number written with a point, where the digits after the point are parts of a whole} \\
  {\textbf{\ealkey{BODMAS}}} \ealnextcol {Height above or below a reference level, especially sea level.} \\
  {\textbf{\ealkey{bus stop method}}} \ealnextcol {The position of a digit after the decimal point.} \\
  {\textbf{\ealkey{decimal}}} \ealnextcol {The dot that separates the whole part from the part less than one.} \\
  {\textbf{\ealkey{decimal place}}} \ealnextcol {A number's distance from zero, written without a plus or minus sign.} \\
  {\textbf{\ealkey{decimal point}}} \ealnextcol {A whole number that can be split exactly into two equal whole numbers.} \\
  {\textbf{\ealkey{divisor}}} \ealnextcol {The size of a number ignoring its sign; the same as absolute value.} \\
  {\textbf{\ealkey{even}}} \ealnextcol {A written layout for dividing, with the dividing number outside the number being split.} \\
  {\textbf{\ealkey{factor}}} \ealnextcol {A written layout for multiplying, using a grid with diagonal lines.} \\
  {\textbf{\ealkey{lattice method}}} \ealnextcol {The number that another number is being divided by.} \\
  {\textbf{\ealkey{magnitude}}} \ealnextcol {how many times a number is multiplied by itself} \\
  {\textbf{\ealkey{negative}}} \ealnextcol {A whole number that cannot be split exactly into two equal whole numbers.} \\
  {\textbf{\ealkey{odd}}} \ealnextcol {A whole number greater than 1 whose only factors are 1 and itself.} \\
  {\textbf{\ealkey{operation}}} \ealnextcol {a whole number that divides exactly into another number} \\
  {\textbf{\ealkey{positive}}} \ealnextcol {The amount left over after dividing a number into equal groups.} \\
  {\textbf{\ealkey{power}}} \ealnextcol {A number with digits after a decimal point that repeat forever.} \\
  {\textbf{\ealkey{prime number}}} \ealnextcol {Less than zero.} \\
  {\textbf{\ealkey{recurring decimal}}} \ealnextcol {Greater than zero.} \\
  {\textbf{\ealkey{remainder}}} \ealnextcol {A mathematical action on numbers, such as adding, subtracting, multiplying or dividing.} \\
  {\textbf{\ealkey{sign}}} \ealnextcol {A number with no part after the decimal point.} \\
  {\textbf{\ealkey{whole number}}} \ealnextcol {The plus or minus symbol that shows whether a number is above or below zero.} \\
}

\newcommand{\ealmatchbody}{%
  \ealstart{\large\bfseries Match the word to its meaning}\par\medskip
  \ealstart\begin{tikzpicture}
    \matrix (m) [ealmatchgrid] {\ealmatchrows};
  \end{tikzpicture}}

\newcommand{\ealanswerbody}{%
  \ealstart{\large\bfseries Match the word to its meaning: answers}\par\medskip
  \ealstart\begin{tikzpicture}
    \matrix (m) [ealmatchgrid] {\ealmatchrows};
    \draw[ealanswerline] (m-1-1.east) -- (m-6-2.west);
    \draw[ealanswerline] (m-2-1.east) -- (m-3-2.west);
    \draw[ealanswerline] (m-3-1.east) -- (m-1-2.west);
    \draw[ealanswerline] (m-4-1.east) -- (m-9-2.west);
    \draw[ealanswerline] (m-5-1.east) -- (m-2-2.west);
    \draw[ealanswerline] (m-6-1.east) -- (m-4-2.west);
    \draw[ealanswerline] (m-7-1.east) -- (m-5-2.west);
    \draw[ealanswerline] (m-8-1.east) -- (m-11-2.west);
    \draw[ealanswerline] (m-9-1.east) -- (m-7-2.west);
    \draw[ealanswerline] (m-10-1.east) -- (m-15-2.west);
    \draw[ealanswerline] (m-11-1.east) -- (m-10-2.west);
    \draw[ealanswerline] (m-12-1.east) -- (m-8-2.west);
    \draw[ealanswerline] (m-13-1.east) -- (m-18-2.west);
    \draw[ealanswerline] (m-14-1.east) -- (m-13-2.west);
    \draw[ealanswerline] (m-15-1.east) -- (m-20-2.west);
    \draw[ealanswerline] (m-16-1.east) -- (m-19-2.west);
    \draw[ealanswerline] (m-17-1.east) -- (m-12-2.west);
    \draw[ealanswerline] (m-18-1.east) -- (m-14-2.west);
    \draw[ealanswerline] (m-19-1.east) -- (m-17-2.west);
    \draw[ealanswerline] (m-20-1.east) -- (m-16-2.west);
    \draw[ealanswerline] (m-21-1.east) -- (m-22-2.west);
    \draw[ealanswerline] (m-22-1.east) -- (m-21-2.west);
  \end{tikzpicture}}


\begin{document}
\ealpage{\ealkeybody}
\ealpage{\ealmatchbody}
\ealpage{\ealanswerbody}
\end{document}
