% !TeX program = lualatex
% ================================================================
% Tier 3 Vocabulary Key for SequencesStartersPatterns
%
% Generated by the server using the following settings:
%
%   built from          latex/starters/targeted/KS3/sequences/sequencesStartersPatterns.tex
%   tier 3 vocabulary   green   RGB 0 128 0
%   English text        black   RGB 0 0 0
%   translated text     purple  RGB 112 48 160
%   layout macros       version 2
%   key layout          version 3
%
% Please don't edit any information in this comment block - the server
% compares it against the current settings and rebuilds this file when
% they differ, so changes here would be overwritten. However, feel free
% to make updates to the LaTeX below if you want to edit it.
% ================================================================
% ==== VOCABULARY DATA - generated, do not edit ====
% {"terms":[{"en":"common difference","def":"the fixed amount added or subtracted to go from one term to the next in a sequence.","tr":null,"trdef":null},
% {"en":"Fibonacci","def":"a famous sequence where each number is the sum of the two numbers before it.","tr":null,"trdef":null},
% {"en":"multiple","def":"a number you get by multiplying another number by a whole number","tr":null,"trdef":null},
% {"en":"nth term","def":"a rule that gives any number in a sequence from its position","tr":null,"trdef":null},
% {"en":"pattern","def":"a regular arrangement of numbers or shapes that follows a rule.","tr":null,"trdef":null},
% {"en":"rule","def":"a consistent way to work out the next number in a sequence.","tr":null,"trdef":null},
% {"en":"sequence","def":"a list of numbers that follows a rule","tr":null,"trdef":null},
% {"en":"square number","def":"a number made by multiplying a whole number by itself, such as 1, 4, 9 or 16.","tr":null,"trdef":null},
% {"en":"sum","def":"the answer when numbers are added together","tr":null,"trdef":null},
% {"en":"term","def":"one part of an expression or one number in a sequence","tr":null,"trdef":null},
% {"en":"triangular number","def":"a number made by adding the whole numbers from 1 upward, such as 1, 3, 6 or 10.","tr":null,"trdef":null}]}
% ==== END VOCABULARY DATA ====

\documentclass[11pt]{article}
\usepackage[a4paper,margin=18mm]{geometry}
\newcommand{\ealtext}[1]{#1}
\newcommand{\ealtextblock}[1]{{\raggedright #1\par}}
% ================================================================
% EAL layout helpers
% ================================================================
\usepackage{xcolor}

\definecolor{ealkeycolour}{RGB}{0,128,0}
\definecolor{ealenglish}{RGB}{0,0,0}
\definecolor{ealtwocolour}{RGB}{112,48,160}

\newsavebox{\ealboxen}
\newsavebox{\ealboxtr}
\newlength{\ealglosswd}
\newlength{\ealglossraise}
\setlength{\ealglossraise}{1.15em}

% a tier 3 word, in English and in translation alike
\newcommand{\ealkey}[1]{\textcolor{ealkeycolour}{#1}}
\newcommand{\ealkeytr}[1]{\textcolor{ealkeycolour}{\ealtext{#1}}}

% One translated word above one English word. Built from kernel
% primitives, never a tabular, which would break wherever the sheet
% loads array, booktabs or tabularx. The box is as wide as the wider of
% the two, so a long translation pushes its neighbours aside instead of
% overlapping them, and it claims its full height so a table row or a
% TikZ node makes room for it.
\newcommand{\ealgloss}[2]{%
  \sbox{\ealboxen}{\ealkey{#1}}%
  \sbox{\ealboxtr}{\small\textcolor{ealtwocolour}{\ealtext{#2}}}%
  \setlength{\ealglosswd}{\wd\ealboxen}%
  \ifdim\wd\ealboxtr>\ealglosswd\setlength{\ealglosswd}{\wd\ealboxtr}\fi
  \leavevmode
  \makebox[\ealglosswd][c]{%
    \makebox[0pt][c]{\usebox{\ealboxen}}%
    \makebox[0pt][c]{\raisebox{\ealglossraise}{\usebox{\ealboxtr}}}%
  }%
}

% opens up line spacing around a block of glosses, so the raised
% translations sit clear of the line above
\newenvironment{ealglossed}{\par\linespread{2.1}\selectfont\ignorespaces}{\par}

% a whole translated sentence above its English counterpart. block level,
% so it does not belong inside a tabular cell
\newcommand{\ealpara}[2]{%
  \par\addvspace{0.5\baselineskip}%
  {\color{ealtwocolour}\ealtextblock{#1}}%
  \penalty200
  {\color{ealenglish}#2\par}%
  \addvspace{0.5\baselineskip}%
}

% ================================================================
% Vocabulary key layout
% ================================================================
\usepackage{tikz}
\usetikzlibrary{matrix}

\colorlet{ealrulecolour}{ealkeycolour!35!white}

\newlength{\ealwordcol}
\setlength{\ealwordcol}{40mm}
\newlength{\ealmeaningcol}
\setlength{\ealmeaningcol}{\dimexpr\textwidth-\ealwordcol-3mm\relax}

\newcommand{\ealentry}[2]{%
  \par\addvspace{1.6mm}%
  \noindent
  \makebox[\ealwordcol][l]{\textbf{\ealkey{#1}}\hspace{2mm}%
    \textcolor{ealrulecolour}{\dotfill}}%
  \hspace{3mm}%
  \parbox[t]{\ealmeaningcol}{\raggedright\strut #2}%
  \par
}

% The translated word and then its meaning, all inside one \ealtext
% rather than two. A right to left language then orders the word, the
% dash and the meaning itself; set as two runs, the word lands at the
% far end of the line and is read last.
%
% The tie keeps the dash with the word, so a meaning that wraps breaks
% after the dash rather than opening its second line with one.
\newcommand{\ealtrmeaning}[2]{%
  \ealtext{{\bfseries\color{ealkeycolour}#1}~\textemdash{} #2}}

% how a heading and the match-up grid sit on the page
\newcommand{\ealstart}{}

% As many copies of a body as fit go on one page, with a cut line
% between them, so a key of five terms is printed four to a sheet
% rather than taking one to itself. A body too tall to fit is typeset
% directly instead, so it breaks across pages rather than overflowing.
\newsavebox{\ealcopybox}
\newlength{\ealcopyht}
\newlength{\ealcutgap}
\setlength{\ealcutgap}{10mm}
\newcount\ealfits
\newcount\ealdone

\newcommand{\ealcut}{%
  \par\vspace{0.5\ealcutgap}%
  \noindent\hbox to \textwidth{%
    \color{ealrulecolour}%
    \leaders\hbox to 6pt{\hss\rule[0.55ex]{3pt}{0.4pt}\hss}\hfill}%
  \par\vspace{0.5\ealcutgap}%
}

\newcommand{\ealpage}[1]{%
  \sbox{\ealcopybox}{\parbox{\textwidth}{#1}}%
  \setlength{\ealcopyht}{\dimexpr\ht\ealcopybox+\dp\ealcopybox\relax}%
  \ifdim\ealcopyht>\textheight
    #1%
  \else
    \ealfits=\dimexpr\textheight+\ealcutgap\relax
    \ealdone=\dimexpr\ealcopyht+\ealcutgap\relax
    \divide\ealfits by \ealdone
    \ifnum\ealfits<1 \ealfits=1 \fi
    \ealdone=0
    \loop\ifnum\ealdone<\ealfits
      \ifnum\ealdone>0 \ealcut\fi
      \noindent\usebox{\ealcopybox}\par
      \advance\ealdone by 1
    \repeat
  \fi
  \newpage
}

\tikzset{
  ealmatchgrid/.style={
    matrix of nodes, ampersand replacement=\ealnextcol,
    row sep=4mm, column sep=24mm,
    nodes={anchor=west, inner sep=1pt},
    column 1/.style={nodes={text width=38mm, align=left}},
    column 2/.style={nodes={text width=82mm, align=left}},
  },
  ealanswerline/.style={ealkeycolour!70!white, line width=0.7pt},
}

\pagestyle{empty}
\setlength{\parindent}{0pt}

\newcommand{\ealkeybody}{%
  \ealstart{\large\bfseries SequencesStartersPatterns: key vocabulary}\par\medskip
  \ealentry{common difference}{the fixed amount added or subtracted to go from one term to the next in a sequence.}
  \ealentry{Fibonacci}{a famous sequence where each number is the sum of the two numbers before it.}
  \ealentry{multiple}{a number you get by multiplying another number by a whole number}
  \ealentry{nth term}{a rule that gives any number in a sequence from its position}
  \ealentry{pattern}{a regular arrangement of numbers or shapes that follows a rule.}
  \ealentry{rule}{a consistent way to work out the next number in a sequence.}
  \ealentry{sequence}{a list of numbers that follows a rule}
  \ealentry{square number}{a number made by multiplying a whole number by itself, such as 1, 4, 9 or 16.}
  \ealentry{sum}{the answer when numbers are added together}
  \ealentry{term}{one part of an expression or one number in a sequence}
  \ealentry{triangular number}{a number made by adding the whole numbers from 1 upward, such as 1, 3, 6 or 10.}
}


\newcommand{\ealmatchrows}{%
  {\textbf{\ealkey{common difference}}} \ealnextcol {a rule that gives any number in a sequence from its position} \\
  {\textbf{\ealkey{Fibonacci}}} \ealnextcol {the fixed amount added or subtracted to go from one term to the next in a sequence.} \\
  {\textbf{\ealkey{multiple}}} \ealnextcol {a list of numbers that follows a rule} \\
  {\textbf{\ealkey{nth term}}} \ealnextcol {the answer when numbers are added together} \\
  {\textbf{\ealkey{pattern}}} \ealnextcol {a consistent way to work out the next number in a sequence.} \\
  {\textbf{\ealkey{rule}}} \ealnextcol {a regular arrangement of numbers or shapes that follows a rule.} \\
  {\textbf{\ealkey{sequence}}} \ealnextcol {a famous sequence where each number is the sum of the two numbers before it.} \\
  {\textbf{\ealkey{square number}}} \ealnextcol {a number you get by multiplying another number by a whole number} \\
  {\textbf{\ealkey{sum}}} \ealnextcol {one part of an expression or one number in a sequence} \\
  {\textbf{\ealkey{term}}} \ealnextcol {a number made by adding the whole numbers from 1 upward, such as 1, 3, 6 or 10.} \\
  {\textbf{\ealkey{triangular number}}} \ealnextcol {a number made by multiplying a whole number by itself, such as 1, 4, 9 or 16.} \\
}

\newcommand{\ealmatchbody}{%
  \ealstart{\large\bfseries Match the word to its meaning}\par\medskip
  \ealstart\begin{tikzpicture}
    \matrix (m) [ealmatchgrid] {\ealmatchrows};
  \end{tikzpicture}}

\newcommand{\ealanswerbody}{%
  \ealstart{\large\bfseries Match the word to its meaning: answers}\par\medskip
  \ealstart\begin{tikzpicture}
    \matrix (m) [ealmatchgrid] {\ealmatchrows};
    \draw[ealanswerline] (m-1-1.east) -- (m-2-2.west);
    \draw[ealanswerline] (m-2-1.east) -- (m-7-2.west);
    \draw[ealanswerline] (m-3-1.east) -- (m-8-2.west);
    \draw[ealanswerline] (m-4-1.east) -- (m-1-2.west);
    \draw[ealanswerline] (m-5-1.east) -- (m-6-2.west);
    \draw[ealanswerline] (m-6-1.east) -- (m-5-2.west);
    \draw[ealanswerline] (m-7-1.east) -- (m-3-2.west);
    \draw[ealanswerline] (m-8-1.east) -- (m-11-2.west);
    \draw[ealanswerline] (m-9-1.east) -- (m-4-2.west);
    \draw[ealanswerline] (m-10-1.east) -- (m-9-2.west);
    \draw[ealanswerline] (m-11-1.east) -- (m-10-2.west);
  \end{tikzpicture}}


\begin{document}
\ealpage{\ealkeybody}
\ealpage{\ealmatchbody}
\ealpage{\ealanswerbody}
\end{document}
