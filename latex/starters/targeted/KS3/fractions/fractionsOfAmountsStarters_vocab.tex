% !TeX program = lualatex
% ================================================================
% Tier 3 Vocabulary Key for FractionsOfAmountsStarters
%
% Generated by the server using the following settings:
%
%   built from          latex/starters/targeted/KS3/fractions/fractionsOfAmountsStarters.tex
%   tier 3 vocabulary   green   RGB 0 128 0
%   English text        black   RGB 0 0 0
%   translated text     purple  RGB 112 48 160
%   layout macros       version 2
%   key layout          version 3
%
% Please don't edit any information in this comment block - the server
% compares it against the current settings and rebuilds this file when
% they differ, so changes here would be overwritten. However, feel free
% to make updates to the LaTeX below if you want to edit it.
% ================================================================
% ==== VOCABULARY DATA - generated, do not edit ====
% {"terms":[{"en":"area model","def":"a rectangle divided into equal parts to represent how fractions multiply.","tr":null,"trdef":null},
% {"en":"bar model","def":"a drawing of a bar split into equal parts to show the quantities in a problem.","tr":null,"trdef":null},
% {"en":"cancel","def":"to simplify a fraction by removing a shared number from its top and bottom.","tr":null,"trdef":null},
% {"en":"common denominator","def":"a shared bottom number used when adding or comparing fractions.","tr":null,"trdef":null},
% {"en":"convert","def":"to change a number from one form into another, like an improper fraction to a mixed number.","tr":null,"trdef":null},
% {"en":"counterexample","def":"an example that proves a general statement is false.","tr":null,"trdef":null},
% {"en":"denominator","def":"the number below the line in a fraction, showing how many equal parts the whole is split into","tr":null,"trdef":null},
% {"en":"fraction","def":"a number written as one whole split into equal parts, with a number of those parts taken","tr":null,"trdef":null},
% {"en":"highest common factor","def":"the largest whole number that divides exactly into two or more numbers","tr":null,"trdef":null},
% {"en":"improper fraction","def":"a fraction whose top number is greater than or equal to its bottom number.","tr":null,"trdef":null},
% {"en":"mixed number","def":"a number made from a whole amount and a fraction, such as 1 1/2.","tr":null,"trdef":null},
% {"en":"multiply","def":"to add a number to itself a given number of times","tr":null,"trdef":null},
% {"en":"numerator","def":"the number above the line in a fraction, showing how many equal parts are taken","tr":null,"trdef":null},
% {"en":"product","def":"the answer when two or more numbers are multiplied","tr":null,"trdef":null},
% {"en":"proper fraction","def":"a fraction whose top number is smaller than its bottom number.","tr":null,"trdef":null},
% {"en":"remainder","def":"the number left over after making equal groups from a larger number.","tr":null,"trdef":null},
% {"en":"simplify","def":"to write something in its smallest or plainest form without changing its value","tr":null,"trdef":null},
% {"en":"whole number","def":"the entire original amount that a fraction is taken from.","tr":null,"trdef":null}]}
% ==== END VOCABULARY DATA ====

\documentclass[11pt]{article}
\usepackage[a4paper,margin=18mm]{geometry}
\newcommand{\ealtext}[1]{#1}
\newcommand{\ealtextblock}[1]{{\raggedright #1\par}}
% ================================================================
% EAL layout helpers
% ================================================================
\usepackage{xcolor}

\definecolor{ealkeycolour}{RGB}{0,128,0}
\definecolor{ealenglish}{RGB}{0,0,0}
\definecolor{ealtwocolour}{RGB}{112,48,160}

\newsavebox{\ealboxen}
\newsavebox{\ealboxtr}
\newlength{\ealglosswd}
\newlength{\ealglossraise}
\setlength{\ealglossraise}{1.15em}

% a tier 3 word, in English and in translation alike
\newcommand{\ealkey}[1]{\textcolor{ealkeycolour}{#1}}
\newcommand{\ealkeytr}[1]{\textcolor{ealkeycolour}{\ealtext{#1}}}

% One translated word above one English word. Built from kernel
% primitives, never a tabular, which would break wherever the sheet
% loads array, booktabs or tabularx. The box is as wide as the wider of
% the two, so a long translation pushes its neighbours aside instead of
% overlapping them, and it claims its full height so a table row or a
% TikZ node makes room for it.
\newcommand{\ealgloss}[2]{%
  \sbox{\ealboxen}{\ealkey{#1}}%
  \sbox{\ealboxtr}{\small\textcolor{ealtwocolour}{\ealtext{#2}}}%
  \setlength{\ealglosswd}{\wd\ealboxen}%
  \ifdim\wd\ealboxtr>\ealglosswd\setlength{\ealglosswd}{\wd\ealboxtr}\fi
  \leavevmode
  \makebox[\ealglosswd][c]{%
    \makebox[0pt][c]{\usebox{\ealboxen}}%
    \makebox[0pt][c]{\raisebox{\ealglossraise}{\usebox{\ealboxtr}}}%
  }%
}

% opens up line spacing around a block of glosses, so the raised
% translations sit clear of the line above
\newenvironment{ealglossed}{\par\linespread{2.1}\selectfont\ignorespaces}{\par}

% a whole translated sentence above its English counterpart. block level,
% so it does not belong inside a tabular cell
\newcommand{\ealpara}[2]{%
  \par\addvspace{0.5\baselineskip}%
  {\color{ealtwocolour}\ealtextblock{#1}}%
  \penalty200
  {\color{ealenglish}#2\par}%
  \addvspace{0.5\baselineskip}%
}

% ================================================================
% Vocabulary key layout
% ================================================================
\usepackage{tikz}
\usetikzlibrary{matrix}

\colorlet{ealrulecolour}{ealkeycolour!35!white}

\newlength{\ealwordcol}
\setlength{\ealwordcol}{40mm}
\newlength{\ealmeaningcol}
\setlength{\ealmeaningcol}{\dimexpr\textwidth-\ealwordcol-3mm\relax}

\newcommand{\ealentry}[2]{%
  \par\addvspace{1.6mm}%
  \noindent
  \makebox[\ealwordcol][l]{\textbf{\ealkey{#1}}\hspace{2mm}%
    \textcolor{ealrulecolour}{\dotfill}}%
  \hspace{3mm}%
  \parbox[t]{\ealmeaningcol}{\raggedright\strut #2}%
  \par
}

% The translated word and then its meaning, all inside one \ealtext
% rather than two. A right to left language then orders the word, the
% dash and the meaning itself; set as two runs, the word lands at the
% far end of the line and is read last.
%
% The tie keeps the dash with the word, so a meaning that wraps breaks
% after the dash rather than opening its second line with one.
\newcommand{\ealtrmeaning}[2]{%
  \ealtext{{\bfseries\color{ealkeycolour}#1}~\textemdash{} #2}}

% how a heading and the match-up grid sit on the page
\newcommand{\ealstart}{}

% As many copies of a body as fit go on one page, with a cut line
% between them, so a key of five terms is printed four to a sheet
% rather than taking one to itself. A body too tall to fit is typeset
% directly instead, so it breaks across pages rather than overflowing.
\newsavebox{\ealcopybox}
\newlength{\ealcopyht}
\newlength{\ealcutgap}
\setlength{\ealcutgap}{10mm}
\newcount\ealfits
\newcount\ealdone

\newcommand{\ealcut}{%
  \par\vspace{0.5\ealcutgap}%
  \noindent\hbox to \textwidth{%
    \color{ealrulecolour}%
    \leaders\hbox to 6pt{\hss\rule[0.55ex]{3pt}{0.4pt}\hss}\hfill}%
  \par\vspace{0.5\ealcutgap}%
}

\newcommand{\ealpage}[1]{%
  \sbox{\ealcopybox}{\parbox{\textwidth}{#1}}%
  \setlength{\ealcopyht}{\dimexpr\ht\ealcopybox+\dp\ealcopybox\relax}%
  \ifdim\ealcopyht>\textheight
    #1%
  \else
    \ealfits=\dimexpr\textheight+\ealcutgap\relax
    \ealdone=\dimexpr\ealcopyht+\ealcutgap\relax
    \divide\ealfits by \ealdone
    \ifnum\ealfits<1 \ealfits=1 \fi
    \ealdone=0
    \loop\ifnum\ealdone<\ealfits
      \ifnum\ealdone>0 \ealcut\fi
      \noindent\usebox{\ealcopybox}\par
      \advance\ealdone by 1
    \repeat
  \fi
  \newpage
}

\tikzset{
  ealmatchgrid/.style={
    matrix of nodes, ampersand replacement=\ealnextcol,
    row sep=4mm, column sep=24mm,
    nodes={anchor=west, inner sep=1pt},
    column 1/.style={nodes={text width=38mm, align=left}},
    column 2/.style={nodes={text width=82mm, align=left}},
  },
  ealanswerline/.style={ealkeycolour!70!white, line width=0.7pt},
}

\pagestyle{empty}
\setlength{\parindent}{0pt}

\newcommand{\ealkeybody}{%
  \ealstart{\large\bfseries FractionsOfAmountsStarters: key vocabulary}\par\medskip
  \ealentry{area model}{a rectangle divided into equal parts to represent how fractions multiply.}
  \ealentry{bar model}{a drawing of a bar split into equal parts to show the quantities in a problem.}
  \ealentry{cancel}{to simplify a fraction by removing a shared number from its top and bottom.}
  \ealentry{common denominator}{a shared bottom number used when adding or comparing fractions.}
  \ealentry{convert}{to change a number from one form into another, like an improper fraction to a mixed number.}
  \ealentry{counterexample}{an example that proves a general statement is false.}
  \ealentry{denominator}{the number below the line in a fraction, showing how many equal parts the whole is split into}
  \ealentry{fraction}{a number written as one whole split into equal parts, with a number of those parts taken}
  \ealentry{highest common factor}{the largest whole number that divides exactly into two or more numbers}
  \ealentry{improper fraction}{a fraction whose top number is greater than or equal to its bottom number.}
  \ealentry{mixed number}{a number made from a whole amount and a fraction, such as 1 1/2.}
  \ealentry{multiply}{to add a number to itself a given number of times}
  \ealentry{numerator}{the number above the line in a fraction, showing how many equal parts are taken}
  \ealentry{product}{the answer when two or more numbers are multiplied}
  \ealentry{proper fraction}{a fraction whose top number is smaller than its bottom number.}
  \ealentry{remainder}{the number left over after making equal groups from a larger number.}
  \ealentry{simplify}{to write something in its smallest or plainest form without changing its value}
  \ealentry{whole number}{the entire original amount that a fraction is taken from.}
}


\newcommand{\ealmatchrows}{%
  {\textbf{\ealkey{area model}}} \ealnextcol {a drawing of a bar split into equal parts to show the quantities in a problem.} \\
  {\textbf{\ealkey{bar model}}} \ealnextcol {the number below the line in a fraction, showing how many equal parts the whole is split into} \\
  {\textbf{\ealkey{cancel}}} \ealnextcol {a rectangle divided into equal parts to represent how fractions multiply.} \\
  {\textbf{\ealkey{common denominator}}} \ealnextcol {an example that proves a general statement is false.} \\
  {\textbf{\ealkey{convert}}} \ealnextcol {a fraction whose top number is greater than or equal to its bottom number.} \\
  {\textbf{\ealkey{counterexample}}} \ealnextcol {to change a number from one form into another, like an improper fraction to a mixed number.} \\
  {\textbf{\ealkey{denominator}}} \ealnextcol {a shared bottom number used when adding or comparing fractions.} \\
  {\textbf{\ealkey{fraction}}} \ealnextcol {to simplify a fraction by removing a shared number from its top and bottom.} \\
  {\textbf{\ealkey{highest common factor}}} \ealnextcol {a number written as one whole split into equal parts, with a number of those parts taken} \\
  {\textbf{\ealkey{improper fraction}}} \ealnextcol {the number above the line in a fraction, showing how many equal parts are taken} \\
  {\textbf{\ealkey{mixed number}}} \ealnextcol {a fraction whose top number is smaller than its bottom number.} \\
  {\textbf{\ealkey{multiply}}} \ealnextcol {to write something in its smallest or plainest form without changing its value} \\
  {\textbf{\ealkey{numerator}}} \ealnextcol {to add a number to itself a given number of times} \\
  {\textbf{\ealkey{product}}} \ealnextcol {the largest whole number that divides exactly into two or more numbers} \\
  {\textbf{\ealkey{proper fraction}}} \ealnextcol {the number left over after making equal groups from a larger number.} \\
  {\textbf{\ealkey{remainder}}} \ealnextcol {a number made from a whole amount and a fraction, such as 1 1/2.} \\
  {\textbf{\ealkey{simplify}}} \ealnextcol {the entire original amount that a fraction is taken from.} \\
  {\textbf{\ealkey{whole number}}} \ealnextcol {the answer when two or more numbers are multiplied} \\
}

\newcommand{\ealmatchbody}{%
  \ealstart{\large\bfseries Match the word to its meaning}\par\medskip
  \ealstart\begin{tikzpicture}
    \matrix (m) [ealmatchgrid] {\ealmatchrows};
  \end{tikzpicture}}

\newcommand{\ealanswerbody}{%
  \ealstart{\large\bfseries Match the word to its meaning: answers}\par\medskip
  \ealstart\begin{tikzpicture}
    \matrix (m) [ealmatchgrid] {\ealmatchrows};
    \draw[ealanswerline] (m-1-1.east) -- (m-3-2.west);
    \draw[ealanswerline] (m-2-1.east) -- (m-1-2.west);
    \draw[ealanswerline] (m-3-1.east) -- (m-8-2.west);
    \draw[ealanswerline] (m-4-1.east) -- (m-7-2.west);
    \draw[ealanswerline] (m-5-1.east) -- (m-6-2.west);
    \draw[ealanswerline] (m-6-1.east) -- (m-4-2.west);
    \draw[ealanswerline] (m-7-1.east) -- (m-2-2.west);
    \draw[ealanswerline] (m-8-1.east) -- (m-9-2.west);
    \draw[ealanswerline] (m-9-1.east) -- (m-14-2.west);
    \draw[ealanswerline] (m-10-1.east) -- (m-5-2.west);
    \draw[ealanswerline] (m-11-1.east) -- (m-16-2.west);
    \draw[ealanswerline] (m-12-1.east) -- (m-13-2.west);
    \draw[ealanswerline] (m-13-1.east) -- (m-10-2.west);
    \draw[ealanswerline] (m-14-1.east) -- (m-18-2.west);
    \draw[ealanswerline] (m-15-1.east) -- (m-11-2.west);
    \draw[ealanswerline] (m-16-1.east) -- (m-15-2.west);
    \draw[ealanswerline] (m-17-1.east) -- (m-12-2.west);
    \draw[ealanswerline] (m-18-1.east) -- (m-17-2.west);
  \end{tikzpicture}}


\begin{document}
\ealpage{\ealkeybody}
\ealpage{\ealmatchbody}
\ealpage{\ealanswerbody}
\end{document}
